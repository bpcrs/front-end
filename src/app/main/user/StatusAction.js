import React, { useState, useEffect } from "react";
import { makeStyles, Grid } from "@material-ui/core";
import { theme } from "@chakra-ui/core";

const useStyles = makeStyles((theme) => ({}));

export default function StatusAction(props) {
  const { booking } = props;

  return <Grid></Grid>;
}
